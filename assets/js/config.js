window.AppConfig = {
  // For local development keep localhost:3000.
  // For production, replace with your deployed backend URL (no trailing slash).
  apiBaseUrl: "https://solowebproject-production.up.railway.app"
};
